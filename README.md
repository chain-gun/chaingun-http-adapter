# gun-lmdb

HTTP adapter for gunDB

Currently read only

## Installation

    npm install @notabug/gun-lmdb

## Usage

    require("@notabug/gun-http").attachToGun(Gun, {
      root: "/gun/nodes/"
    });
